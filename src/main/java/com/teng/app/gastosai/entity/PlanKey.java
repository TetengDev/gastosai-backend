package com.teng.app.gastosai.entity;

/** Subscription tiers. {@link #FREE} is the implicit default when a user has no active subscription. */
public enum PlanKey {
    FREE,
    PREMIUM,
    TRIAL
}
