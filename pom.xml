<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.1</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.teng.app</groupId>
	<artifactId>gastosai</artifactId>
	<version>0.93.2</version>
	<name/>
	<description/>
	<url/>
	<licenses>
		<license>
			<name>Proprietary — All Rights Reserved</name>
			<url>https://github.com/TetengDev/gastosai/blob/master/LICENSE</url>
			<distribution>manual</distribution>
			<comments>Proprietary software. See the LICENSE file at the repository root.</comments>
		</license>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>
	<properties>
		<java.version>25</java.version>
		<!-- Coverage floor (TEN-396, ../docs/coverage.md). The goal is 90% line coverage of
		     business logic; the gate is this floor, which may only ever rise. Both values are
		     JaCoCo ratios (0.81 = 81%), set from a measured `./mvnw clean verify` run and rounded
		     DOWN to a whole percent — never estimated, never rounded up, never lowered. A PR that
		     raises coverage raises these; a PR that deletes tests fails here.

		     Measured 2026-09-18 over the denominator pinned on the JaCoCo plugin below:
		     82.75% lines (4829/5836), 71.79% branches (1621/2258). Lowering either number is
		     only ever legitimate together with a *wider* denominator, and then it is not a
		     lowering at all — it is a different measurement.

		     TEN-401 raised the line floor from 0.81 to 0.82 by covering ChatActionService's write
		     paths. The branch floor stays at 0.68 for now: that issue's scope was the line floor,
		     and raising the branch one is the next coverage issue's to claim with its own
		     measurement. -->
		<coverage.line.min>0.82</coverage.line.min>
		<coverage.branch.min>0.68</coverage.branch.min>
	</properties>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-cache</artifactId>
		</dependency>
		<dependency>
			<groupId>com.github.ben-manes.caffeine</groupId>
			<artifactId>caffeine</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-redis</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springdoc</groupId>
			<artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
			<version>3.1.1</version>
		</dependency>

		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-devtools</artifactId>
			<scope>runtime</scope>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>io.github.cdimascio</groupId>
			<artifactId>dotenv-java</artifactId>
			<version>3.2.0</version>
		</dependency>
		<dependency>
			<groupId>org.apache.commons</groupId>
			<artifactId>commons-csv</artifactId>
			<version>1.14.1</version>
		</dependency>
		<!-- PDF export (TEN-179). PDFBox is a plain library: Apache 2.0, no Spring coupling and
		     no Framework-7 surface to be incompatible with, and it logs through slf4j-api which
		     Boot already provides. Chosen over OpenPDF/iText, whose LGPL/AGPL split is a licensing
		     question this proprietary repo does not need to answer. -->
		<dependency>
			<groupId>org.apache.pdfbox</groupId>
			<artifactId>pdfbox</artifactId>
			<version>3.0.8</version>
		</dependency>
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-mail</artifactId>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-api</artifactId>
			<version>0.13.0</version>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-impl</artifactId>
			<version>0.13.0</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>io.jsonwebtoken</groupId>
			<artifactId>jjwt-jackson</artifactId>
			<version>0.13.0</version>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-validation-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-webmvc-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-flyway</artifactId>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-core</artifactId>
		</dependency>
		<dependency>
			<groupId>org.flywaydb</groupId>
			<artifactId>flyway-database-postgresql</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-test</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- Real PostgreSQL for the whole test suite. H2 in PostgreSQL mode emulates casing,
		     information_schema, partial indexes, ON CONFLICT and interval/timestamp arithmetic
		     differently enough that a repository change could pass CI and fail in production, so
		     it is gone from the test scope entirely — see src/test/resources/application.properties
		     for how the one shared container is wired. Versions come from the Boot parent's
		     Testcontainers BOM. -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-testcontainers</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers-postgresql</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers-junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- Core-only circuit breaker (no Spring Boot starter) - guards external LLM provider calls.
		     spring-boot4 starter exists (2.4.0) but core-only keeps the footprint minimal on 512 MB. -->
		<dependency>
			<groupId>io.github.resilience4j</groupId>
			<artifactId>resilience4j-circuitbreaker</artifactId>
			<version>2.4.0</version>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<configuration>
					<excludes>
						<exclude>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
						</exclude>
					</excludes>
				</configuration>
				<executions>
					<execution>
						<id>build-info</id>
						<goals>
							<goal>build-info</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<version>0.8.15</version>
				<!-- The denominator, pinned. A coverage number means nothing without it, so every
				     exclusion is named here one by one — no wildcard that quietly grows and
				     flatters the next reading. Applied at plugin level so `report` and `check`
				     share one denominator.

				     The test is behaviour, not the @Configuration annotation: only the Spring
				     entry point and the two config classes whose bodies really are nothing but
				     bean declarations are excluded. JacksonTimeConfig's outer class registers a
				     module and nothing else — its inner serializer/deserializer hold the actual
				     logic and stay measured, since `X.class` does not match `X$Inner.class`.

				     Deliberately NOT excluded, though all are @Configuration:
				       SecurityConfig   builds the authorization boundary — versionedAdminRules(),
				                        the matcher factories, and the ordering that puts
				                        public-permit ahead of admin-deny.
				       WebConfig        wires the AI-key, AI rate-limit, public rate-limit and
				                        authenticated-write interceptors to their path patterns,
				                        and normalises the CORS origin list.
				       AIClientConfig   carries the BYO-key-over-platform-key fallback inside the
				                        OpenAI and Claude request interceptors.
				       CacheConfig      decides whether caching is on, and its evictor is
				                        tenant-scoped.
				       EmailSenderConfig picks Resend over SMTP.
				       OpenApiConfig    marks each published path public or secured.
				     Excluding any of them would let a PR that deletes the tests over that logic
				     still meet the floor with none of it counted. -->
				<configuration>
					<excludes>
						<exclude>com/teng/app/gastosai/GastosaiApplication.class</exclude>
						<exclude>com/teng/app/gastosai/config/JacksonTimeConfig.class</exclude>
						<exclude>com/teng/app/gastosai/config/PayMongoRestClientConfig.class</exclude>
					</excludes>
				</configuration>
				<executions>
					<execution>
						<id>prepare-agent</id>
						<goals><goal>prepare-agent</goal></goals>
					</execution>
					<execution>
						<id>report</id>
						<phase>verify</phase>
						<goals><goal>report</goal></goals>
					</execution>
					<!-- The gate itself: `./mvnw verify` fails below the floor. Bound after the
					     report so a failure can be read off target/site/jacoco/index.html. -->
					<execution>
						<id>check-coverage-floor</id>
						<phase>verify</phase>
						<goals><goal>check</goal></goals>
						<configuration>
							<rules>
								<rule>
									<element>BUNDLE</element>
									<limits>
										<limit>
											<counter>LINE</counter>
											<value>COVEREDRATIO</value>
											<minimum>${coverage.line.min}</minimum>
										</limit>
										<limit>
											<counter>BRANCH</counter>
											<value>COVEREDRATIO</value>
											<minimum>${coverage.branch.min}</minimum>
										</limit>
									</limits>
								</rule>
							</rules>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<executions>
					<execution>
						<id>default-compile</id>
						<phase>compile</phase>
						<goals>
							<goal>compile</goal>
						</goals>
						<configuration>
							<annotationProcessorPaths>
								<path>
									<groupId>org.projectlombok</groupId>
									<artifactId>lombok</artifactId>
								</path>
							</annotationProcessorPaths>
						</configuration>
					</execution>
					<execution>
						<id>default-testCompile</id>
						<phase>test-compile</phase>
						<goals>
							<goal>testCompile</goal>
						</goals>
						<configuration>
							<annotationProcessorPaths>
								<path>
									<groupId>org.projectlombok</groupId>
									<artifactId>lombok</artifactId>
								</path>
							</annotationProcessorPaths>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

</project>
